<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title><%= title %></title>
    <link href="/css/style.css" rel="stylesheet">
  </head>
  <body>
    <h1><%= title %></h1>
    <div class="center">
    <form class="add-update" action="/<%= movie.id %>/update?_method=PUT" method="POST">
      <label for="name">Name</label>
      <input type="text" name="name" id="name" value="<%= movie.name %>" required>
      <label for="genre">Genre</label>
      <input type="text" name="genre" id="genre" value="<%= movie.genre %>" required>
      <label for="year">Year</label>
      <input type="text" name="year" id="year" value="<%= movie.year %>" required>
      <label for="director">Director</label>
      <input type="text" name="director" id="director" value="<%= movie.director %>" required>
      <button type="submit">Update</button>
    </form>
    </div>
    <a class="home" href="/">Back to Home</a>
  </body>
</html>
